spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5555/micro_chats
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import: configserver:http://localhost:8888
GATEWAY_URL_BASE: ""
