spring:
  application:
    name: 'micro-chats'
  config:
    import: optional:configserver:http://localhost:8888/
  profiles:
    active: dev
  cloud:
    function:
      definition: submitMessage
    stream:
      bindings:
#        submitMessage-out-o:
#          destination:
        submitMessage-out-0:
          destination: input
          group: create
      rabbit:
        bindings:
#          submitMessage-in-0:
#            consumer:
#              exchange-type: topic
          submitMessage-out-0:
            producer:
              exchange-type: topic
logging:
  level:
    org.springframework.cloud: warn
    org.springframework.cloud.stream: info
    org.springframework.security: trace
service:
  key: Uieiej6895op